# Copy this file to .env and add local settings as they are introduced.
# Optional local run-history database
RETAILFLOW_STORAGE__DATABASE_URL=sqlite:///retailflow.sqlite3

# Local mock API only. Never commit production tokens.
RETAIL_API_URL=http://127.0.0.1:8000
RETAIL_API_TOKEN=retailflow-demo-token
